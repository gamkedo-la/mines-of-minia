export { ProcEnemy };

class ProcEnemy {
    // need to do the following:
    // -- pick level
    // -- pick health based on base dice and level * health per level dice
    // -- pick damage based on base dice and level * damage per level dice

}
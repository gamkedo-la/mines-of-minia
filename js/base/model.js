export { Model };

import { Rect } from './rect.js';
import { UxView } from './uxView.js';

class Model extends UxView {

    // STATIC VARIABLES ----------------------------------------------------
    static cat = 'Model';

    // CONSTRUCTOR ---------------------------------------------------------
    cpost(spec={}) {
        super.cpost(spec);
    }

}
